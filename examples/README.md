# Examples

| Example | Purpose |
|---------|---------|
| [minimal](./minimal/) | Smoke-test **`kove-context`** (no OCI provider). |

Add **`examples/<name>/`** as separate Terraform roots when introducing networking, OKE, or full stacks.
