# Examples

| Example | Purpose |
|---------|---------|
| [minimal](./minimal/) | Smoke-test **`labels`** (no OCI provider). |
| [oke](./oke/) | Deploy OKE using **`labels` + `networking` + `oke`** modules. |
| [rdma-platform](./rdma-platform/) | Deploy the full RDMA stack from `modules/rdma-platform` (same stack wrapper available under `stacks/kove-rdma-platform`). |
