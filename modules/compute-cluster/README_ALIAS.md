This directory is a non-breaking alias copy of `modules/rdma-platform`.

Preferred organization goal: split deployment patterns by module name.

Legacy path (`rdma-platform`) remains available for compatibility.
