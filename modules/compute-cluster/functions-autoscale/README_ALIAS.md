This directory is a non-breaking alias copy of `modules/rdma-autoscale`.

Preferred naming going forward: **functions-autoscale**.

Legacy path (`rdma-autoscale`) remains available for compatibility.
